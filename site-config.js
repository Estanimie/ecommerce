window.PISTA_NERA_CONFIG = {
  // Si el frontend queda en Netlify y el backend en otro host, pega aqui la URL publica del backend.
  // Ejemplo: apiBaseUrl: "https://tu-backend.onrender.com"
  apiBaseUrl: ""
};
